apiVersion: v1
kind: Secret
metadata:
  name: solidfire-secret
  namespace: solidfire-csi
type: Opaque
stringData:
  # Credentials for the SolidFire Cluster Admin
  username: "admin"
  password: "admin" # Using stringData handles the base64 encoding automatically!
  endpoint: "192.168.1.34"
  tenant: "k0s"
