apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-purge
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: solidfire-purge